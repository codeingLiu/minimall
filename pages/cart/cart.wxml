<!--pages/cart/cart.wxml-->
<view class="cart">
 <scroll-view class="cart-list" scroll-y>
   <block wx:for="{{cartList}}" wx:key="index">
    <m-select goods="{{item}}" index="{{index}}"></m-select>
   </block>
 </scroll-view>
 
 <bottom-bar></bottom-bar>
</view>
